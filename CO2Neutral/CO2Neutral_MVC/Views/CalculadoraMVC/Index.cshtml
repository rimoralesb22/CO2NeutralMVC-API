@model IEnumerable<CO2Neutral_MVC.Models.Calculadora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.contrasenna)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idCalculdora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.resultadoTransporte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.resultadoElectricidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.resultadoReciclaje)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.resultadoGas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.contrasenna)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idCalculdora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.resultadoTransporte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.resultadoElectricidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.resultadoReciclaje)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.resultadoGas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.idCalculdora }) |
            @Html.ActionLink("Details", "Details", new { id = item.idCalculdora }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.idCalculdora })
        </td>
    </tr>
}

</table>
